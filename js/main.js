const title='AudioX';const el=document.getElementById('typed-title');let i=0;function type(){if(i<=title.length){el.textContent=title.slice(0,i++);setTimeout(type,125)}}type();
const button=document.getElementById('download-button');const status=document.getElementById('download-status');button.addEventListener('click',()=>{status.textContent='Preparing download…';button.disabled=true;setTimeout(()=>{const blob=new Blob(['AudioX Preview\nv0.1.0\n\nThis is the website release placeholder. Replace this file with the actual AudioX build when ready.'],{type:'text/plain'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download='AudioX-v0.1.0-preview.txt';a.click();URL.revokeObjectURL(url);status.textContent='✓ Download started';button.disabled=false},700)});
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');if(e.target.classList.contains('type-on-view')&&!e.target.dataset.typed){e.target.dataset.typed='true';const text=e.target.dataset.text||e.target.textContent;e.target.textContent='';let n=0;const run=()=>{if(n<=text.length){e.target.textContent=text.slice(0,n++);setTimeout(run,45)}};run()}}),{threshold:.18});document.querySelectorAll('.reveal,.type-on-view').forEach(x=>observer.observe(x));
const cards=document.querySelectorAll('.tilt-card');cards.forEach(card=>{card.addEventListener('mousemove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(700px) rotateX(${-y*5}deg) rotateY(${x*5}deg) translateY(-3px)`});card.addEventListener('mouseleave',()=>card.style.transform='')});
const magnetic=document.querySelector('.magnetic');if(magnetic){magnetic.addEventListener('mousemove',e=>{const r=magnetic.getBoundingClientRect();magnetic.style.transform=`translate(${(e.clientX-r.left-r.width/2)*.12}px,${(e.clientY-r.top-r.height/2)*.12}px)`});magnetic.addEventListener('mouseleave',()=>magnetic.style.transform='')}
const folder=document.getElementById('sound-folder');const list=document.getElementById('sound-list');if(folder&&list){folder.addEventListener('change',()=>{list.innerHTML='';const files=[...folder.files].filter(f=>f.type.startsWith('audio/'));if(!files.length){list.innerHTML='<span>No supported audio files found.</span>';return}files.forEach(file=>{const row=document.createElement('div');row.className='sound-row';row.innerHTML=`<span>♫</span><strong>${file.webkitRelativePath||file.name}</strong><button type="button">Preview</button>`;const audio=new Audio(URL.createObjectURL(file));row.querySelector('button').onclick=()=>{document.querySelectorAll('.sound-row audio').forEach(a=>a.pause());audio.currentTime=0;audio.play()};row.appendChild(audio);list.appendChild(row)})})}
